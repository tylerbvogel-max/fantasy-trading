# Session Notes — Feb 19, 2026

## Completed

### Wanted Level Progress Bar
- **Commit**: `3371a88` — "Replace wanted level text with progress bar in header"
- Replaced plain `Lv.3 (4x)` text in the header with a horizontal progress bar
- `WantedBar` component: 120x16px, `Colors.surface` track, `Colors.orange` fill, `Radius.full` rounded
- Fill = `level / 10 * 100%` (capped at 100%), label `Lv.{level} · {mult}x` absolutely positioned over the bar
- Replaced in all 3 game states (swiping, locked-in, waiting)
- Removed unused `wantedText` style
- TypeScript clean (pre-existing error in HexIronBar props unrelated — see below)

## In Progress (Uncommitted Changes)

There are several uncommitted changes across multiple files from a previous session:

| File | What changed |
|------|-------------|
| `mobile/src/screens/BountyHunterScreen.tsx` | Iron boost system: `BOOST_EFFECTS` map, `boostedIronId` state, `getIronEffects()` now accepts `boostedIronId`, passes boost props to `HexIronBar` |
| `mobile/src/components/HexIronBar.tsx` | Major rework — now expects `boostedIronId` and `onBoostSelected` props (this causes the pre-existing TS error since the 3 call sites in BountyHunterScreen don't all pass these yet) |
| `mobile/src/api/client.ts` | +1 line (unknown — check diff) |
| `backend/app/services/bounty_config.py` | ~70 lines added |
| `backend/app/services/bounty_service.py` | ~9 lines changed |
| `docs/ideas-and-decisions.md` | +3 lines |

### TypeScript Error to Fix
```
src/screens/BountyHunterScreen.tsx(917,10): error TS2739:
  Type '{ chambers: ...; }' is missing the following properties from type 'Props':
  boostedIronId, onBoostSelected
```
The `HexIronBar` component now requires `boostedIronId` and `onBoostSelected` props, but one of the three render sites in BountyHunterScreen doesn't pass them yet. Fix by adding the missing props at the call site around line 917.

## Blocked: Can't Log Into App on Phone

### Problem
Tyler got kicked from the game and can't register or log in via Expo Go.

### Root Cause
The mobile app points at the **deployed Render server** (`fantasy-trading-api.onrender.com`), not localhost:
- `mobile/app.json` → `expo.extra.apiUrl` = `https://fantasy-trading-api.onrender.com`
- `mobile/src/api/client.ts` line 11 reads this value

Invite codes and test users created locally don't exist on the remote DB.

### Next Steps (pick one)
1. **Point app at local backend for testing** — change `apiUrl` in `mobile/app.json` to `http://<local-ip>:8000`, restart Expo. Local invite codes will work. Remember to change back before deploying.
2. **Create invite code on remote DB** — need access to the Render PostgreSQL database (connection string from Render dashboard). Then run:
   ```sql
   INSERT INTO invite_codes (code, max_uses, times_used, created_at)
   VALUES ('TEST-LOGIN', 99, 0, NOW());
   ```
3. **Add an admin endpoint to create invite codes** — hit the remote server via curl to create codes on demand.
4. **Use the existing admin account** — if there's an admin user seeded on the remote DB, find its credentials and log in with that, then create invite codes through the app/API.

### Local Test Accounts (only work against localhost)
- `tyler_test` / token: `test123`
- `tyler2` / token: `KIYpxT5yiAENVXc6MS2BVWsTVCbywryyd1WKO2ZrARw`
- Available local invite code: `BETA-BC834D`
